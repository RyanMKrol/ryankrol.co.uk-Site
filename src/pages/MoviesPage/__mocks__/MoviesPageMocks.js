import MoviesPage from '../MoviesPage';

const FULL_SIZE_MOCK = <MoviesPage fullSize={true} />;
const LIMITED_SIZE_MOCK = <MoviesPage fullSize={false} />;

export { FULL_SIZE_MOCK, LIMITED_SIZE_MOCK };
