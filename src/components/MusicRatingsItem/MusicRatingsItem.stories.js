import React from 'react';

import DEFAULT_MOCK from './__mocks__';

export default {
  title: 'Components/MusicRatingsItem',
};

/**
 * default story
 *
 * @returns {React.Component} JSX for the MusicRatingsItem
 */
export const normal = () => DEFAULT_MOCK;
