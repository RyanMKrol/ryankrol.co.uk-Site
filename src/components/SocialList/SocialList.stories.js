import React from 'react';

import SocialList from './SocialList';

export default {
  title: 'Components/SocialList',
};

/**
 * default story
 *
 * @returns {React.Component} JSX for the SocialList
 */
export const normal = () => <SocialList />;
